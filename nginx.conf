events {}

http {
  include mime.types;

  backend {
    listen 80;

    location /graphql {
      proxy_pass http://backend:4000;
    }

    location / {
      root /web-client-build;
      try_files $uri /index.html;
    }
  }
}